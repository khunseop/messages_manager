# Messages Manager

회사 메신저 백업 자동화 툴입니다. DRM이 걸린 MHT 파일을 Word를 통해 열어서 파싱하고 정리합니다.

## 기능
- MHT 파일 읽기 및 파싱 (Word Application 활용)
- 문단 및 표 데이터 추출 (순서 유지)
- Markdown 형식으로 저장

## 요구 사항
- Windows OS (Word 설치 필요)
- Python 3.x
- `pywin32` 라이브러리 (`pip install pywin32`)
